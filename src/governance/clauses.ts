// Clause extraction scaffold
export interface Clause {
  id?: string;
  type?: string;
  text?: string;
}

export function extractClauses(doc: any): Clause[] {
  // Placeholder: no extraction yet
  return [];
}
